# 聚會選詩歌 hymn-picker

<https://soaring0616.github.io/hymn-picker/>

幫主持聚會的人預備詩歌：選一個聚會類型，依「類別吻合、會眾熟悉度、有沒有副歌」排出候選，
每一首都附上**推薦理由**，可以直接拿去跟服事的弟兄姊妹討論。

純前端靜態網站：Vite + React 18 + TypeScript，資料全放在 `public/data/*.json`，沒有後端。
這是 [soaring0616.github.io](../README.md) 的一個分頁；原始碼放在這個資料夾，
build 出來的 `dist/` 不進 git，由 repo 根目錄的 GitHub Actions 建置後部署到 `/hymn-picker/`。
**改 `public/data/*.json` 直接 commit + push 就會上線**，本機不需要 Node。

---

## 資料怎麼新增

### `public/data/hymns.json`

一個陣列，每個元素是一首詩歌：

| 欄位 | 必填 | 說明 |
| --- | :-: | --- |
| `id` | ✅ | 全域唯一。`h-` 大本／`s-` 補充本／`n-` hymnal.net 新歌／`c-` 兒童詩歌，後面接號碼，例如 `h-623`。**這是 React 的 `key`，一旦發布就不要再改**。 |
| `book` | ✅ | `hymnal`＝大本（hymnal.net `ch/`，1–780 首）｜`supplement`＝補充本（`ts/`）｜`new`＝hymnal.net 新歌 New Songs（`ns/`；注意 `nt/` 是 New Tunes 新調，不是新歌）｜`children`＝兒童詩歌（hymnal.net 沒有中文版，資料來自蒙特利公園市召會的頁面） |
| `no` | ✅ | 該本裡的號碼（數字，不是字串） |
| `title` | ✅ | 中文標題 |
| `firstLine` |  | 第一句歌詞，卡片上顯示在標題底下幫忙辨認 |
| `categories` | ✅ | 中文類別陣列，例如 `["記念主", "主的救贖"]`。**選詩全靠這個欄位**，見下面的類別詞彙表。 |
| `key` / `time` / `meter` |  | 調號／拍號／韻律，例如 `"降E大調"`、`"4/4"`、`"8.7.8.7 副"` |
| `hasChorus` | ✅ | 有沒有副歌（`true` 會在評分時加分，兒童／福音聚會權重最高） |
| `familiarity` |  | 會眾熟悉度 0–1。不填就依書別預設（大本 0.6／補充本 0.45／新歌 0.3／兒童詩歌 0.5）。不常唱的填低一點（例如 `0.2`）就會往後排，並在理由裡標「會眾較不熟」 |
| `enNo` |  | hymnal.net 的英文號 |
| `urls.hymnal` |  | hymnal.net 網址 |
| `urls.luke54` |  | luke54.org 網址陣列（沒有就放 `[]`） |
| `urls.cimp` |  | 蒙特利公園市召會的詩歌頁網址，兒童詩歌用這個（有歌詞、調號、拍號） |
| `notes` | ✅ | 註解陣列，見下 |
| `ignoreIndex` |  | 設 `true` 就不用詩歌本目錄自動補類別（例如 288 只想留「安慰」）。預設會補，見下面「詩歌本目錄與自動補類別」 |
| `tags` |  | 主題關鍵字（生活情境），例如 `["憂慮", "出遊"]`。大多由 `theme_tags.json` 在載入時掛上，這裡也可以手填；首頁的「主題關鍵字」搜尋會比對 |
| `usage` |  | **不用填**。載入時由 `sunday_picks.json` 算出：主日唱過幾次、各段幾次、最近一次 |

`notes` 的每一則：

| 欄位 | 必填 | 說明 |
| --- | :-: | --- |
| `type` | ✅ | `excerpt` 摘句｜`story` 背景｜`scripture` 經節｜`tip` 帶詩歌提醒 |
| `text` | ✅ | **一到兩句**就好，長篇請放 `url` 連出去 |
| `source` | ✅ | 出處，例如「hymnal.net 歌詞（副歌）」「詩 134:1-3」 |
| `url` |  | 全文連結；沒有就不會出現「閱讀全文 →」 |

新增一首詩歌 = 在陣列尾巴加一個物件，commit、push 就會上線。
TypeScript **不會**檢查 JSON 內容（它是執行期 fetch 進來的），所以欄位打錯要靠自己看畫面。

### 詩歌本目錄與自動補類別

`hymns.json` 的 `categories` 是手填的；另外有兩個檔案讓程式在載入時**自動往上加**類別：

- `public/data/hymn_index.json`：大本 780 首、補充本 437 首、兒童詩歌 330 首在詩歌本目錄裡的位置（大類／細目），
  由 `scripts/build_hymn_index.py` 從
  [蒙特利公園市召會的目錄頁](https://churchinmontereypark.org/Docs/Hymn/firstBookHymnIndex.html)
  （[補充本](https://churchinmontereypark.org/Docs/Hymn/secondBookHymnIndex.html)、[兒童詩歌](https://churchinmontereypark.org/Docs/Hymn/childBookHymnIndex.html)）產生，不要手改。
  補充本目錄頁不齊全，但補充本的號碼本身就分段（1xx 靈與生命、2xx 享受基督、4xx 追求與長大…），
  缺的號碼程式會用百位數推回大類。
- `public/data/category_map.json`：目錄大類（或「大類／細目」）→ 本專案類別。
  例如 `"讚美主／祂的受苦": ["記念主", "主的救贖"]`、`"鼓勵": ["加強", "安慰"]`、`"追求與長大": ["受成全", "渴慕"]`。
  兒童詩歌的大類跟大本會撞名（都有「傳揚福音」），所以鍵一律寫成 `"兒童詩歌／主的愛"`，而且每一條都帶 `兒童`。
  這張表是判斷題，**改它就改了整本詩歌的歸類**，請帶詩歌的人一起看。

合併規則（`src/lib/hymnIndex.ts`）：先查「大類／細目」再查「大類」，命中的取聯集；
手填的類別排前面、目錄補的排後面；某首詩不想被目錄影響就設 `ignoreIndex: true`。
卡片上會多一行「目錄 讚美主／祂的受苦」，讓帶詩歌的人知道它在詩歌本裡的位置。

### 主題關鍵字 `public/data/theme_tags.json`

類別（`categories`）是「這首詩講什麼」，主題關鍵字（`tags`）是「什麼時候會想唱它」：憂慮、出遊、晨興、職場、婚姻、畢業……
一個出處一筆：誰點名了哪幾首、談什麼主題。

```json
{ "id": "EP41", "label": "合聲響應 EP41 沐浴主愛系列", "url": "https://www.youtube.com/watch?v=…",
  "tags": ["主愛", "憂慮", "安慰", "重擔"], "hymns": ["h-286", "h-232", "s-328"] }
```

載入時（`src/lib/themeTags.ts`）把 tags 掛到 `hymn.tags`，同一首被多個出處點名就取聯集，出處放進 `hymn.tagSources`，
卡片上顯示成 `# 憂慮` 這種黃色 chip 和「主題出處」連結。另外還有一個「自由關鍵字」輸入框，比對標題、首句、類別和 tags，只加分不過濾。

**跟聚會類型怎麼搭**：聚會類型是硬過濾（include／exclude），主題關鍵字在那之後再篩。首頁把所有 tags 列成可勾選的 chip，
chip 上的數字是「在目前這種聚會裡」帶此主題的首數，切換聚會會跟著變，0 首的 chip 直接不顯示（已勾選的除外）；
勾一個以上就只列帶這些主題的詩歌，命中越多排越前。勾了卻一首都沒有時，畫面會列出這些主題在別種聚會各有幾首，點一下就切過去。
chip 依 `groups` 分成八列：心情處境（憂慮、孤單、試煉…）、場合（出遊、晨興、開學、聖誕…）、聖經人物與書卷（亞伯拉罕、雅歌、詩篇二十三篇…）、
羨慕（渴慕與長進：羨慕基督、脫離己、主的引領…，對應詩歌本的「羨慕」類）、愛主與交通（愛主、呼求主名、喫喝主…）、奉獻與跟隨（奉獻、跟隨主、爭戰…）、
召會與建造（建造、錫安、國度…）、帶詩提醒（容易唱錯、難唱、新調、外語…）；沒歸組的放「其他」。同一個 tag 只放一組。`hymns` 可以先列還沒加進 hymns.json 的 id，加進來就自動生效。

目前的出處是水深・火熱的兩個節目：《合聲響應》45 集（id `EP…`）和《與詩有約》86 集（id `YS…`），
tags 依每集的系列名與標題整理，例如「心煩憂鬱不慌張」→ 心煩、憂鬱、難關；「與亞伯拉罕有約」→ 亞伯拉罕、信心。
要加別的出處（例如某次特會的詩歌單）就在 `sources` 尾巴加一筆。

### 主日豫選紀錄 `public/data/sunday_picks.json`

主日擘餅每週四段「調靈 → 讚美主 → 記念主 → 敬拜父」各一首的紀錄（有些週沒有調靈）。一週一筆：

```json
{ "date": "2025-03-02", "picks": [
  { "slot": "調靈", "hymn": "h-200", "morningRevival": true },
  { "slot": "讚美主", "hymn": "h-376" }, { "slot": "記念主", "hymn": "h-181" }, { "slot": "敬拜父", "hymn": "h-8" }
] }
```

載入時（`src/lib/sundayPicks.ts`）做兩件事：每首詩掛上 `usage`（唱過幾次、各段幾次）；把唱過的段落名依次數插到 `categories` **最前面**，
所以擘餅推薦的分段會照你們實際的用法（178 唱過 13 次記念主，就一定在記念主段）。
主日在某段唱過的詩也**不受擘餅的 exclude 限制**（調靈段常點福音、渴慕類的詩，例如補充本 836、410）。
刻意**不**拿唱過的次數去改熟悉度、也不在卡片上顯示次數：每個地方對同一首詩的熟悉度不一樣，這份紀錄只代表一處召會的用法。
頁首的「主日豫選紀錄」可以整份翻閱。
新的一週在 `weeks` 尾巴加一筆即可；還沒收進 hymns.json 的詩會被略過。

### `public/data/meeting_types.json`

| 欄位 | 說明 |
| --- | --- |
| `id` / `label` | 內部代號／畫面上顯示的名字 |
| `include` | 允許的類別。**空陣列 = 不限**（出遊、特別聚會、自訂就是這樣） |
| `exclude` | 排除的類別，命中任一個整首排掉（優先於 `include`） |
| `weights` | `familiarity` 熟悉度（只看書別：大本 > 補充本 > 新歌）／`chorus` 有副歌／`themeMatch` 類別吻合。會自動正規化成總和 1，所以寫 `3/2/5` 或 `0.3/0.2/0.5` 效果一樣 |
| `sections` | 分段，例如擘餅的「調靈 → 讚美主 → 記念主 → 敬拜父」。每段有自己的 `include`，但沿用聚會層級的 `exclude` 與 `weights`。**同一首詩只會出現在一個段落**：`categories` 的第一個類別命中哪段就歸哪段（主日唱過的段落、手填的主要類別都在最前面）；第一個沒命中時歸命中最多的段，平手看命中類別誰在 `categories` 裡排得前面。`include` 為空陣列的段落是「一般」段：收下沒命中任何主題段的詩，再用聚會層級的 `include` 過濾（小排／訓練／特會就是「願意受成全 → 加強 → 一般」） |
| `suggestedCount` | 建議選幾首（決定畫面上先顯示幾張卡片，其餘收在「再多看幾首」後面） |

目前的類別詞彙（`categories` 與 `include`/`exclude` 要用同一組字，多一個空格就對不上）：

> 記念主、主的救贖、基督的所是、敬拜父、讚美主、感恩、召會生活、教會、聖靈、
> 經歷神、安慰、渴慕、獻上自己、禱告、祈求、爭戰、福音、傳福音、得救的確據、
> 聖經、青年、兒童、受成全、加強、調靈

`受成全`（願意受成全）與 `加強` 是小排／訓練／特會的主題段落用的；擘餅聚會把這兩類排除，因為訓練、特會的詩歌不適合擘餅唱。
`調靈` 是擘餅第一段（調靈）用的，目前只由主日豫選紀錄產生，不用手填。

---

## 專案結構

```
hymn-picker/
├─ public/data/
│  ├─ hymns.json            # 詩歌主資料（目前 369 首）
│  ├─ hymn_index.json       # 詩歌本目錄（大本 780 + 補充本 437 + 兒童詩歌 330 首的大類／細目），腳本產生
│  ├─ category_map.json     # 目錄大類 → 本專案類別的對照表（手工維護）
│  ├─ theme_tags.json       # 主題關鍵字：哪個出處點名了哪些詩、談什麼主題（手工維護）
│  ├─ sunday_picks.json     # 主日擘餅每週四段的豫選紀錄（手工維護，每週加一筆）
│  └─ meeting_types.json    # 12 種聚會的選詩規則
├─ scripts/
│  └─ build_hymn_index.py   # 從目錄網頁產生 hymn_index.json（只用 Python 標準函式庫）
├─ src/
│  ├─ types.ts              # 所有型別定義（資料的合約）
│  ├─ lib/
│  │  ├─ select.ts          # 純函式：過濾 + 評分 + 排序
│  │  ├─ select.test.ts     # vitest 測試
│  │  ├─ hymnIndex.ts       # 純函式：用目錄 + 對照表補類別
│  │  ├─ hymnIndex.test.ts
│  │  ├─ themeTags.ts        # 純函式：掛主題關鍵字
│  │  ├─ themeTags.test.ts
│  │  ├─ sundayPicks.ts      # 純函式：掛主日唱過的紀錄
│  │  ├─ sundayPicks.test.ts
│  │  └─ data.ts            # 載入 JSON（用 BASE_URL 組路徑），載入時順便補類別
│  ├─ components/
│  │  ├─ HymnCard.tsx       # 詩歌卡片（含最完整的教學註解）
│  │  ├─ MeetingSelector.tsx
│  │  └─ HymnList.tsx       # 結果清單：先只列號碼＋曲名，點一列才展開卡片
│  ├─ pages/
│  │  ├─ Home.tsx           # 選聚會 → 看推薦
│  │  ├─ HymnDetail.tsx     # 單首詳細頁（註解 + 外部連結）
│  │  └─ SundayPicks.tsx    # 主日豫選紀錄（一週一列）
│  ├─ App.tsx               # HashRouter 與路由表、頁首（含「回首頁」連結）
│  └─ main.tsx              # 進入點
└─ vite.config.ts           # base 預設 /hymn-picker/，可用 VITE_BASE 蓋掉

（部署用的 workflow 在 repo 根目錄 ../.github/workflows/deploy.yml，不在這個資料夾裡）
```

技術棧：Vite 4 + React 18 + TypeScript + react-router-dom 6（HashRouter）+ vitest。
樣式用 CSS Modules（`*.module.css`），沒有 UI 框架、沒有 Tailwind。

## 資料來源與校正狀態

`hymns.json` 目前的 369 首：大本、補充本、新歌共 343 首，標題、第一句、調號、拍號、韻律、有無副歌、英文號與 `notes` 裡的
摘句、作者資料，都是直接從 <https://www.hymnal.net> 對應頁面抄下來的（前 27 首 2026-09-06，之後的 43 首 2026-09-12）；
兒童詩歌 26 首（`c-`）抄自蒙特利公園市召會的兒童詩歌頁，兒童聚會的 `include` 現在只有 `兒童`，所以兒童聚會只會列兒童詩歌和特別標了 `兒童` 的大本詩歌。
**`categories` 是依歌詞內容與 hymnal.net 的分類初步對到本專案的類別詞彙**，帶詩歌的人請再校對。
`urls.luke54` 目前都是空陣列，沒有查證過的連結就不放。

`notes` 裡 `type: "tip"` 的帶詩歌提醒，以及部分作者背景，出自
[1000首詩歌 1000個吉他手](https://1000hymns.blogspot.com/search/label/%E5%94%B1%E8%A9%A9%E4%BA%BA%EF%BC%86%E5%8F%B8%E7%90%B4%E8%80%85%E5%BF%85%E8%AE%80)
「唱詩人＆司琴者必讀」標籤下的文章（擘餅點詩的 9 大禁忌、李常受弟兄談聚會中的 10 種詩歌與選詩原則等），
每則都附了原文連結。第二批的 21 首就是這些文章點名的詩歌。

第三批 17 首是為了補原本幾乎沒有詩歌的類別挑的：福音／傳福音（大本 571、664、722、724、731、733，補充本 818、830、842、876）、
聖靈（大本 202、203、212）、召會生活／教會（大本 621，補充本 713、714、720），並順手把 `兒童`、`青年` 標在幾首簡短好唱的上面。
這批每首多了一則 `scripture` 經節，`familiarity` 是憑經驗填的，請依會眾實況調整。

第四批 26 首是帶詩歌的弟兄姊妹點名的：敬拜父段用大本 1–12、31；擘餅記念主段加 127、135；
小排／訓練／特會的「願意受成全」段是大本 203、204、209、210 與補充本 410、433，「加強」段目前只有大本 212；
大本 217、218、227、230、231、232 放在這三種聚會的「一般」段（227 也標了福音）；241 給福音聚會。
大本 288「與我同住」只留 `安慰` 一類，所以只會在小排（和不限類別的聚會）出現。

第五批 87 首是《合聲響應》各集點名的詩歌（大本 31、補充本 53、兒童詩歌 3），資料同樣從 hymnal.net／蒙特利公園市召會抄下來並核對編號；
這批的 `categories` 一律留空，全靠詩歌本目錄自動補。集數標題裡有兩處號碼標錯（「補充本 737」其實是大本 737〈愛何大尋回我〉、「補充本 253」不是〈耶穌耶穌耶穌〉），已依實際內容修正。

第六批 119 首是《與詩有約》100 集點名的詩歌（大本 60、補充本 56、兒童詩歌 3），作法同第五批。

第七批 73 首是主日豫選紀錄裡唱過、但還沒收錄的（大本 59、補充本 14），作法同上。
同時把 126、128、130、135 的 `categories` 改成記念主在前（帶詩歌的人指定它們在記念主段）；127、134 依主日紀錄實際是唱在讚美主段，就跟著紀錄走。

新增詩歌時的兩個提醒：

- hymnal.net 對**不存在的號碼不會回 404**，而是隨機回一首字序打亂的詩歌。
  抓資料時要確認頁面上的編號標籤（例如 `C623`、`Cs201`、`NS1158`）跟你要的號碼一致，且歌詞是通順的。
  大本在 hymnal.net 只到 780 首，超過的號碼一律是這種誘餌頁。
- 早期的假資料曾把大本 786、新歌 1290 列進來，這兩個號碼在 hymnal.net 上都不存在，已移除。
